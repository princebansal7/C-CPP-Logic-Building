#include <math.h>
#include <stdio.h>
int main()
{
    // Menu Driven Program
    int year;

    printf("Enter year: ");
    scanf("%d", &year);

    switch (year) {
    // Yet to do
    case 1900 ... 3000:
        printf("%d\n", year);
    }
    return 0;
}